# TEST: 修复 AutoClip 导出设置中 FFmpeg 编辑预设按钮无响应

- **Change ID**: fix-autoclip-ffmpeg-edit-preset
- **测试日期**: 2026-06-10

## 本次测试范围声明

| 轮次             | 状态    | 范围         | 跳过理由                                   |
| ---------------- | ------- | ------------ | ------------------------------------------ |
| 第 1 轮 · 功能   | ⚠️ 部分 | UAT 手动验证 | 纯 UI 交互修复，5 行代码，无新增逻辑可单测 |
| 第 2 轮 · 性能   | ❌ 跳过 | —            | 无渲染/网络/计算路径变更                   |
| 第 3 轮 · 安全   | ❌ 跳过 | —            | 纯客户端 UI 修复，无安全边界变更           |
| 第 4 轮 · 兼容   | ❌ 跳过 | —            | 无浏览器特定 API，无 schema 变更           |
| 第 5 轮 · 可观测 | ❌ 跳过 | —            | 无日志/指标/告警变更                       |

---

## 第 1 轮 · 功能测试

### 1.1 验收线映射

| 验收线                                                                             | 类型   | 用例  | 状态              |
| ---------------------------------------------------------------------------------- | ------ | ----- | ----------------- |
| 选中自定义 FFmpeg 预设后，点击「编辑此预设 →」按钮，弹窗关闭并跳转到 FFmpeg 设置页 | manual | UAT-1 | 🟡 待用户手动验证 |

### 1.2 UAT 脚本

```
UAT-1：编辑 FFmpeg 预设按钮导航
  前置：
    1. 启动应用（Docker 容器 bililive-tools-test）
    2. 打开设置弹窗 → 切片 tab
    3. 点击"编辑预设"按钮 → AutoClipPresetDialog 弹窗打开
    4. 切换到"导出设置" tab
    5. 在 FFmpeg 预设下拉中选中一个自定义预设（非内置，不以 b_ 开头）
  步骤：
    1. 确认预览卡片出现（显示编码器/码率/CRF 等参数）
    2. 确认「编辑此预设 →」按钮可见
    3. 点击「编辑此预设 →」按钮
  期望：
    - AutoClipPresetDialog 弹窗关闭
    - 浏览器 URL 变为 /#/home?tab=ffmpeg-setting
    - 页面显示 FFmpeg 设置 tab（ffmpegSetting 组件）
  通过/失败：待验证
```

### 1.3 TypeScript 编译验证

```
$ npx tsc --noEmit -p packages/app/tsconfig.json
TypeScript: No errors found  ✅
```

---

## 测试质量自检（6 维）

| 维度          | 状态 | 说明                                |
| ------------- | ---- | ----------------------------------- |
| T1 测试晦涩   | N/A  | 无新增测试代码                      |
| T2 测试脆弱   | N/A  | 无新增测试代码                      |
| T3 测试重复   | N/A  | 无新增测试代码                      |
| T4 Mock 滥用  | N/A  | 无新增测试代码                      |
| T5 覆盖率幻觉 | N/A  | 无新增测试代码                      |
| T6 架构错配   | N/A  | 已用 UAT 验证 UI 交互，匹配变更层级 |

---

## 回归测试登记

本次无新增自动化测试用例。手动 UAT 见 1.2。
