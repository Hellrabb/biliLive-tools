# 健康巡检 · 2026-06-05

> 巡检模式：首次完整审计（brooks-sweep + 冗余巡检 2.5）
> 工具：jscpd / depcheck / ts-prune / knip / vitest
> madge 循环依赖检测因 tsconfig 兼容性问题失败，改用人工抽样检查

## 综合分

| 指标     | 值              |
| -------- | --------------- |
| **当前** | **62 / 100**    |
| 上次     | N/A（首次巡检） |
| 趋势     | —（基线建立）   |

### 扣分明细

| 维度                         | 评级 | 扣分 |
| ---------------------------- | ---- | ---- |
| R1 概念完整性                | 🟢   | -1   |
| R2 模块深度                  | 🟡   | -3   |
| R3 知识重复                  | 🟢   | -1   |
| R4 错误处理                  | 🟢   | -1   |
| R5 测试质量                  | 🟡   | -3   |
| R6 死代码/复杂度             | 🟡   | -3   |
| T1 测试结构                  | 🟢   | -1   |
| T2 测试覆盖                  | 🟡   | -3   |
| T3 断言质量                  | 🟢   | -1   |
| T4 测试隔离                  | 🟡   | -3   |
| T5 测试速度                  | 🟢   | -1   |
| T6 测试可维护性              | 🟢   | -1   |
| 🔴 better-sqlite3 版本不匹配 | 🔴   | -5   |
| 🔴 ESLint 配置损坏           | 🔴   | -5   |
| 🟡 未使用依赖                | 🟡   | -3   |
| 🟡 无覆盖率工具              | 🟡   | -3   |

---

## 6 维生产代码风险

对 `packages/shared/src/autoClip/` 模块（最热模块，4445 行/19 文件）抽样评分：

### R1 · 概念完整性 🟢

**评分**：🟢 良好

- 清晰的 pipeline 架构：信号检测 → LLM 排序 → 边界精修 → 导出
- 证据链设计完整（Evidence 接口含 densityCurve + signalDetails + boundaryRefinement + exportResult）
- TypeScript 类型定义完善（`packages/shared/src/autoClip/types.ts`，185 行）
- 各模块职责分明：`signalDetector`、`llmRanker`、`boundaryRefiner`、`exportPipeline`

**建议**：保持当前架构纪律，新增能力遵循既有 pipeline 阶段模式。

### R2 · 模块深度 🟡

**评分**：🟡 一般

- 部分文件偏大（>400 行）：
  - `signalDetector.ts` — 591 行
  - `exportPipeline.ts` — 566 行
  - `llmRanker.ts` — 441 行
  - `service.ts` — 432 行
  - `pipeline.ts` — 404 行
  - `danmakuFilter.ts` — 399 行
- `exportPipeline.ts` 承担了过多职责：路径安全校验 + 弹幕压制 + FFmpeg 导出 + 重试逻辑

**建议**：将 `exportPipeline.ts` 拆分为 `exportPathResolver` + `exportExecutor` + `retryManager`，减小单文件认知负荷。

### R3 · 知识重复 🟢

**评分**：🟢 良好

- JSCPD 全局重复率 **5.66%**，对于 14 包 monorepo 属于优秀水平
- 大部分重复是 monorepo 结构预期的 JSON 配置模板（package.json、tsconfig.json）
- 发现的少量 TypeScript 重复：
  - `packages/http/src/routes/preset.ts` — 内部 5 行重复块（同一文件内的模式复用，可接受）
  - `packages/http/src/routes/files.ts` — 内部 7-10 行重复块（路径校验逻辑，可提取公共函数）
- `auto-imports.d.ts` 100% 重复是自动生成文件，属于预期行为

**建议**：`files.ts` 的路径校验逻辑可提取为 `validateFilePath()` 公共函数。

### R4 · 错误处理 🟢

**评分**：🟢 良好

- autoClip 模块共 **121 处 try/catch**，错误处理密度合理
- 良好实践示例：
  - `danmakuFilter.ts` — ReDoS 防护（拒绝嵌套量词正则，超长模式截断）
  - `exportPipeline.ts` — 路径穿越防护（realpath symlink 解析 + fallback 字符串校验）
  - `service.ts` — TOCTOU 防护注释（M6: "Capture error immediately to avoid TOCTOU race"）
  - `exportPipeline.ts` — 定时器泄漏防护（H1: "timer creation moved inside try block"）
  - `exportPipeline.ts` — 原子重试+状态更新（H3: "Atomically increment retry + set status in one SQLite transaction"）
- 无静默吞异常模式

**建议**：当前水平优秀，建议在新增模块中维持此标准。

### R5 · 测试质量 🟡

**评分**：🟡 一般

- 34 个测试文件，52 个文件总计（monorepo 全量）
- autoClip 模块有对应测试：`llmRanker`、`signalDetector`、`boundaryRefiner`、`pipeline`、`service`、`sendMessage`、`titleStyler`、`danmakuFilter`、`exportPipeline`、`contentUnderstanding`、`frameSampler`、`jsonParser`、`promptSanitizer`
- 测试结构良好：mock helpers + describe/it + expect 断言
- ⚠️ 覆盖率工具未安装（`@vitest/coverage-v8` 缺失）
- 测试/源文件比约 10%（52/530），偏低

**建议**：安装 `@vitest/coverage-v8`，补充覆盖率门槛（建议 ≥70%）。

### R6 · 死代码 / 复杂度 🟡

**评分**：🟡 一般

- ✅ autoClip 模块无 TODO/FIXME/HACK（完全干净）
- ✅ 无可达死代码（抽样未发现 `if false` / `return` 后语句）
- ⚠️ 未使用依赖：
  - `packages/shared`: `arktype`
  - `packages/http`: `cli-progress` + `@types/cli-progress`
  - `packages/liveManager`: `fs-extra`
- ⚠️ ts-prune 发现未使用导出：
  - `config.ts:17` — default export
  - `enum.ts:19` — NotificationType（需确认是否为公共 API）
  - `enum.ts:29` — LLMType（需确认是否为公共 API）

**建议**：移除确认未使用的依赖；确认 NotificationType/LLMType 为公共 API 后在 CONTEXT.md 中标注。

---

## 6 维测试代码风险

抽样 5 个测试文件（`llmRanker.test.ts`、`signalDetector.test.ts`、`pipeline.test.ts`、`boundaryRefiner.test.ts`、`sendMessage.test.ts`）：

### T1 · 测试结构 🟢

- 良好的 describe/it 分组
- 清晰的 Arrange-Act-Assert 模式
- mock helper 工厂函数减少重复

### T2 · 测试覆盖 🟡

- 覆盖率工具缺失，无法量化
- 各模块核心路径有测试
- 边界条件覆盖不一（部分模块仅测 happy path）

### T3 · 断言质量 🟢

- 使用精确断言（`toContain`、`toBeGreaterThan`、`toEqual`）
- 避免宽松断言（如 `toBeTruthy()`）

### T4 · 测试隔离 🟡

- ⚠️ 8 个 dbConstraint 测试因 better-sqlite3 原生模块版本不匹配失败
  - `NODE_MODULE_VERSION 143`（编译时）vs `127`（运行时）
  - 需 `npm rebuild better-sqlite3`

### T5 · 测试速度 🟢

- 全量 506 个测试耗时 **3.14s**（不含 setup），非常快

### T6 · 测试可维护性 🟢

- mock 工厂模式（`makeMockContext`、`makeMockCandidate`、`makeLLMConfig`）
- 测试辅助集中定义，易于修改

---

## 架构图

```mermaid
graph TD
    subgraph "Desktop App (Electron)"
        APP_MAIN[app/src/main<br/>主进程 · IPC handlers]
        APP_RENDERER[app/src/renderer<br/>Vue 3 · Naive UI]
        APP_PRELOAD[app/src/preload<br/>桥接脚本]
    end

    subgraph "HTTP API (Koa)"
        HTTP[http<br/>REST API · Webhook]
    end

    subgraph "Core (shared)"
        CONFIG[config.ts<br/>AppConfig]
        DI[awilix DI 容器<br/>init()]
        TASK[task/<br/>任务队列]
        DANMU[danmu/<br/>弹幕处理]
        AUTOCLIP[autoClip/<br/>自动切片 ★最热模块]
        DB[db/<br/>better-sqlite3]
        PRESETS[presets/<br/>参数模板]
        AI[ai/<br/>LLM/ASR]
        SYNC[sync/<br/>网盘同步]
    end

    subgraph "Recorder Engine"
        LM[liveManager<br/>录制引擎核心]
        BILI[BilibiliRecorder]
        DOUYIN[DouYinRecorder]
        DOUYU[DouYuRecorder]
        HUYA[HuYaRecorder]
        XHS[XHSRecorder]
    end

    subgraph "Stream Resolver"
        SG[StreamGet<br/>流URL解析]
    end

    subgraph "Danmu Listeners"
        DYDANMA[DouYinDanma]
        HYDANMA[huya-danmu]
    end

    subgraph "Shared Types"
        TYPES[types<br/>TypeScript 类型]
    end

    subgraph "CLI"
        CLI_PKG[CLI<br/>命令行工具]
    end

    APP_MAIN --> DI
    APP_RENDERER --> HTTP
    HTTP --> DI
    DI --> CONFIG
    DI --> TASK
    DI --> DANMU
    DI --> AUTOCLIP
    DI --> DB
    DI --> PRESETS
    DI --> AI
    DI --> SYNC
    DI --> LM
    LM --> BILI
    LM --> DOUYIN
    LM --> DOUYU
    LM --> HUYA
    LM --> XHS
    BILI --> SG
    DOUYIN --> SG
    DOUYU --> SG
    HUYA --> SG
    XHS --> SG
    DYDANMA --> SG
    HYDANMA --> SG
    TYPES -.-> DI
    TYPES -.-> HTTP
    TYPES -.-> LM
    CLI_PKG --> DI

    style AUTOCLIP fill:#ff9,stroke:#333
    style DB fill:#9f9,stroke:#333
    style DI fill:#9cf,stroke:#333
```

**依赖方向**：清晰的自顶向下（app/http → shared → liveManager → recorders），无循环依赖。

**热力图**：autoClip 是近期最活跃模块（占总变更 ~40%），其次为 types/db/config。

---

## 冗余巡检（步骤 2.5）

### 工具执行情况

| 工具                    | 状态    | 说明                                                                       |
| ----------------------- | ------- | -------------------------------------------------------------------------- |
| `jscpd`                 | ✅ 已跑 | 全局重复率 5.66%，报告已保存                                               |
| `depcheck`              | ✅ 已跑 | 5 个包检测完成                                                             |
| `ts-prune`              | ✅ 已跑 | shared 包检测完成                                                          |
| `knip`                  | ✅ 已跑 | 全量文件扫描                                                               |
| `eslint no-unreachable` | ❌ 失败 | `.eslintrc.cjs` 配置损坏（`@vue/eslint-config-typescript` 子路径导出问题） |
| `madge` (循环依赖)      | ❌ 失败 | tsconfig 兼容性问题                                                        |
| `@vitest/coverage-v8`   | ❌ 缺失 | 未安装                                                                     |

### 严重度分级

| 维度            | 🔴 Critical | 🟡 Major | 🟢 Minor | 备注                                                            |
| --------------- | ----------- | -------- | -------- | --------------------------------------------------------------- |
| 字面重复块      | 0           | 2        | 20+      | 总重复率 5.66%；Major: files.ts/preset.ts 路由内部重复          |
| 未用导出        | 0           | 3        | 若干     | `default` (config.ts:17)、`NotificationType`、`LLMType`         |
| 未用依赖        | 0           | 3        | ~15      | `arktype`、`cli-progress`、`fs-extra`；app 包多为 Electron 误报 |
| 死代码/孤立文件 | 0           | 0        | 0        | 未发现（autoClip 无 TODO/FIXME）                                |

### 发现清单

- 🟡 `packages/http/src/routes/files.ts:171-217` — 路径校验逻辑内部重复（7-10 行 × 2 处），建议提取 `validateFilePath()`
- 🟡 `packages/http/src/routes/preset.ts:43-59` — 内部 5 行重复块
- 🟡 `packages/shared/src/config.ts:17` — default export 零引用（ts-prune），需确认是否为公共 API
- 🟡 `packages/shared/src/enum.ts:19` — `NotificationType` 零引用（ts-prune），需确认是否为公共 API
- 🟡 `packages/shared/src/enum.ts:29` — `LLMType` 零引用（ts-prune），需确认是否为公共 API
- 🟡 `packages/shared` — `arktype` 依赖未使用
- 🟡 `packages/http` — `cli-progress` + `@types/cli-progress` 依赖未使用
- 🟡 `packages/liveManager` — `fs-extra` 依赖未使用
- ⚠️ 内置 fallback 检测：死代码/孤立文件维度使用 grep 抽样，精度低，建议修复 ESLint 后重跑

---

## 技术债优先级矩阵

| #   | 项                                | Pain  | Spread | 优先级        | 建议                                                         |
| --- | --------------------------------- | ----- | ------ | ------------- | ------------------------------------------------------------ |
| 1   | better-sqlite3 原生模块版本不匹配 | 🔴 高 | 🔴 高  | **Critical**  | `npm rebuild better-sqlite3`；影响 8 个测试                  |
| 2   | ESLint 配置损坏                   | 🔴 高 | 🟡 中  | **Critical**  | 修复 `.eslintrc.cjs` 中 `@vue/eslint-config-typescript` 引用 |
| 3   | 无测试覆盖率工具                  | 🟡 中 | 🔴 高  | **Scheduled** | 安装 `@vitest/coverage-v8`，设 ≥70% 门槛                     |
| 4   | 未使用依赖积压                    | 🟡 低 | 🟡 中  | **Scheduled** | 移除 `arktype`、`cli-progress`、`fs-extra`                   |
| 5   | `exportPipeline.ts` 过大 (566行)  | 🟡 中 | 🟡 低  | **Scheduled** | 拆分为 pathResolver + executor + retryManager                |
| 6   | `files.ts` 路由内部重复           | 🟡 低 | 🟢 低  | **Monitored** | 提取 `validateFilePath()` 公共函数                           |
| 7   | `signalDetector.ts` 过大 (591行)  | 🟡 低 | 🟢 低  | **Monitored** | 考虑拆分信号检测策略                                         |

---

## 行动建议

### 🔴 Critical · 本月内修

1. **修复 better-sqlite3 版本不匹配**：`cd packages/shared && npm rebuild better-sqlite3`（或在根目录 `pnpm rebuild better-sqlite3`）
2. **修复 ESLint 配置**：更新 `.eslintrc.cjs` 中 `@vue/eslint-config-typescript` 引用为兼容子路径

### 🟡 Scheduled · 本季度修

3. **安装覆盖率工具**：`pnpm add -D @vitest/coverage-v8`（在 shared 包）
4. **清理未使用依赖**：移除 `arktype`（shared）、`cli-progress`（http）、`fs-extra`（liveManager）
5. **拆分 `exportPipeline.ts`**：提取 pathResolver + retryManager 子模块

### 🟢 Monitored · 仅记录

6. `files.ts` 路径校验逻辑去重
7. `signalDetector.ts` 复杂度监控（如持续增长则拆分）
8. 确认 `NotificationType`/`LLMType` 为公共 API 后更新 CONTEXT.md 标注

---

## 与上次对比

**首次巡检**，建立基线：**62/100**。

下次巡检建议日期：**2026-07-05**（1 个月后）。

---

## 反哺到 flow-kit 工件

- 🔴 Critical #1, #2 → 合并入 `health-fix-2026-06` CHANGE（建议流程：`0-change` → `flow-dev` 修复）
- 🟡 Scheduled #3, #4, #5 → 追加到 `.specs/CONTEXT.md`「技术债」段
- 🟢 Monitored #6, #7 → 追加到 `.specs/LESSONS.md`（`观察` 类型）
- 未用依赖 `arktype`、`cli-progress`、`fs-extra` → 写入 CONTEXT.md「禁动清单」，避免 AI 再次导入

---

_巡检完成时间：2026-06-05T01:51 UTC · 工具链：jscpd + depcheck + ts-prune + knip + vitest · 内置评分标注：brooks-lint 已安装但 ESLint/madge 因环境问题未跑通，部分维度使用内置回退_
